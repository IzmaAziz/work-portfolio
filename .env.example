# Optional: base path for static assets and client-side routing.
# - Omit or leave empty for local dev and custom domains (default: /).
# - For GitHub Pages, set to your repo name with leading and trailing slash:
#   VITE_BASE_PATH=/work-portfolio/

# VITE_BASE_PATH=/
